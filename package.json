{
	"name": "low-code",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "cd packages/admin && pnpm dev",
		"husky": "husky install",
		"lint": "biome lint",
		"changelog": "conventional-changelog -p angular -i CHANGELOG.md -s",
		"commit": "git status && git add -A && git-cz && git push"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@biomejs/biome": "^1.8.3",
		"@commitlint/cli": "^18.6.0",
		"@commitlint/config-conventional": "^18.6.0",
		"commitizen": "^4.3.0",
		"cz-git": "^1.9.4",
		"husky": "^9.1.3",
		"lint-staged": "^15.2.7"
	},
	"config": {
		"commitizen": {
			"path": "node_modules/cz-git"
		}
	},
	"husky": {
		"hooks": {
			"pre-commit": "lint-staged"
		}
	},
	"dependencies": {
		"radash": "^12.1.0"
	}
}
