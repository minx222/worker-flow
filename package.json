{
	"name": "low-code",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev:low": "cd packages/low-code && pnpm dev",
		"dev": "cd packages/flow && pnpm dev",
		"lefthook": "lefthook install",
		"lint": "biome lint",
		"changelog": "conventional-changelog -p angular -i CHANGELOG.md -s",
		"commit": "git status && git add -A && git-cz && git push"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@biomejs/biome": "^1.8.3",
		"@commitlint/cli": "^18.6.0",
		"@commitlint/config-conventional": "^18.6.0",
		"commitizen": "^4.3.0",
		"cz-git": "^1.9.4",
		"lefthook": "^1.7.11"
	},
	"dependencies": {
		"radash": "^12.1.0"
	},
	"config": {
		"commitizen": {
			"path": "node_modules/cz-git"
		}
	}
}
